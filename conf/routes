# Authentication
POST    /auth       controllers.AuthController.auth
POST    /send       controllers.SendMessageController.send
GET     /logs       controllers.SendLogController.logs(from: Long ?= 0, to: Long ?= System.currentTimeMillis / 1000)